# Real

Real applications, or tests derived from real applications. These are probably
large enough to have their own command line interface, etc., and will each
probably be a separate swift packages (also to avoid a large dependency
footprint).

